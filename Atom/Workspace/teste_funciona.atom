import core;

function int main(int argc, char argv)
{
    print("Hello World"); -- comentando a linha

    /*
    Testando comentario multi-linha
    vamo ver se funciona mesmo
    */

    int x = 3;
    if(x >= 3) print("x >= 3");

    x = 5;
    if(x <= 5) print("x <= 3");

    x = 7;
    if(x == 5) print("x == 3");

    float f = 32.43;
    
    
    return 0;
}