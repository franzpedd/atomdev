import core;
import math;

enum MyEnum
{
    MY_VERY_FIRST_ENUMERATION,
    MY_VERY_SECOND_ENUMERATION
};

import struct MyStruct
{
    f32 x;
    f32 y;
} MyStruct;

function i32 main(i32 argc, char argv)
{
    i32 x = 32;
    i32 y = 64;

    MyStruct pos = {};
    pos.x = 800.0;
    pos.y = 600.0;

    i32 xy = x + y;
    f32 varf32 = 32.0;
}
